let zero: 0 = 0;
zero = 1;

// =========================================================
// =========================================================

let hi: "hi" = "hi";
hi = "by";

// =========================================================
// =========================================================

const giveAnswer = (answer: "yes" | "no"): string => {
  return `The answer is ${answer}`;
};

giveAnswer("salam");

// =========================================================
// =========================================================

let mood: "Happy" | "Sad" = "Happy";
mood = "Sad";

// =========================================================
// =========================================================

type WeekendDays = "Saturday" | "Sunday";

let today: WeekendDays = "Sunday"
let tomorrow: WeekendDays = "Monday"
